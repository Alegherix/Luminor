import { strings } from "../../strings";
import type { CatalogLabels } from "./threadCatalog";

export const CATALOG_LABELS: CatalogLabels = {
  unfiled: strings.threads.unfiled,
  now: strings.threads.now,
  minutesAgo: strings.threads.minutesAgo,
  hoursAgo: strings.threads.hoursAgo,
  daysAgo: strings.threads.daysAgo,
};
