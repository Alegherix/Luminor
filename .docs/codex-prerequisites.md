# Codex prerequisites

- Install Codex CLI so `codex` is on your PATH.
- Authenticate Codex before running Luminor (for example via API key or ChatGPT auth supported by Codex).
- Luminor starts the server via `codex app-server` per session.
